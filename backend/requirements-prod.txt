# Runtime-only dependencies for the production container (Cloud Run).
# The full requirements.txt also carries dev/test tooling — this trimmed list
# keeps the image small so cold starts stay fast.
fastapi==0.110.1
uvicorn==0.25.0
motor==3.3.1
pymongo==4.5.0
pydantic>=2.6.4
python-dotenv>=1.0.1
requests>=2.31.0
httpx>=0.27.0
anthropic>=0.40.0
hijridate>=2.3.0
adhanpy>=1.0.0
tzdata>=2024.2
